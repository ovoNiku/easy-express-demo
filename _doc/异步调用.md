# 异步调用

有三种写法, 最早的是 callback

之后是 promise

最新的是 async await

这里主要说一下 async 定义的方法如何用 promise 执行


```JavaScript
const func = async function() {return 'hi'}

// 正常调用
await func()

// 用 promise 解决
Promise.resolve(func()).then((r) => console.log(r))
```

func() 本身返回的就是一个 promise